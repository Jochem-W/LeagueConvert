name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  
jobs:
  Build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.5

      - name: Publish
        run: dotnet.exe publish LeagueBulkConvert.WPF -c Release
        
      - name: Get short SHA
        run: Write-Output "name=$($Env:GITHUB_SHA.SubString(0,7))" | Out-File -FilePath $env:GITHUB_ENV -Append;
        
      - name: Upload build artifact
        uses: actions/upload-artifact@v2.2.4
        with:
          name: LeagueBulkConvert-${{ env.name }}
          path: LeagueBulkConvert.WPF/bin/Release/net5.0-windows/publish
